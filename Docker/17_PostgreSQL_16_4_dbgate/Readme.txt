
// ======================================

	Postgres in docker with dbgate tool (can connect ot any database, setting already created in docker-compose.yml file)

1. Build --- only for the first time
    $ docker-compoe bild

2. Run:
    $ docker-compose up

3. Stop
    $ docker-compose down

Open dbgate in browser;
    localhost:8087  --- credentials wrotten in docker-compose.yml